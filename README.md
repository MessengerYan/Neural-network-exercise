# Neural-network-exercise
Exercise on neural networks with PyTorch as part of my studies.

The goal was to train an artificial neural network that minimized the cost function of an image in order to clear the noise on the image.
